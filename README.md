# 🧾 Insurance Charge Prediction (Flask App)

A web app that predicts insurance charges based on user inputs such as age, BMI, smoker status, etc.

### Run locally:
```bash
pip install -r requirements.txt
python app.py
